@using Research_Repository_Utility;

@using Microsoft.AspNetCore.Http;
@inject IHttpContextAccessor HttpContextAccessor;

@model Research_Repository_Models.ViewModels.ThemeVM

@{
    IEnumerable<SelectListItem> tagSelectList = TempData.Get<IEnumerable<SelectListItem>>("tagSelectList");
}

<div class="container p-3">
    <form method="post" enctype="multipart/form-data">
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="text-primary">Theme List</h2>
            </div>
        </div>

        <br /><br />
        <ul>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="" asp-controller="Theme" asp-action="Index">Themes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="" asp-controller="Team" asp-action="Index">Teams</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark item-request-link" asp-area="" asp-controller="ItemRequest" asp-action="Index">Requests</a>
                <span id="item-request-count"></span>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Create admin user</a>
            </li>
        </ul>

        @if (Model.ThemeObjects != null && Model.ThemeObjects.Count() > 0)
        {

            <table class="table table-bordered table-striped" style="width: 100%">
                <thead>
                    <tr>
                        <th>Theme Name</th>
                        <th>Theme tags</th>
                        <th>Buttons</th>
                    </tr>
                </thead>
                <tbody>

                    @for (int y = 0; y < Model.ThemeObjects.Count; y++)
                    {
                        if (Model.ThemeObjects[y].Theme != null)
                        {
                            <tr class="theme">

                                <td width="50%">
                                    <input asp-for="@Model.ThemeObjects[y].Theme.Id" class="theme-id" hidden />
                                    <div class="theme-name-text">@Model.ThemeObjects[y].Theme.Name</div>
                                    <input asp-for="@Model.ThemeObjects[y].Theme.Name" class="form-control theme-name-input" style="display:none;" />
                                    <button class="edit-theme-name-btn" onclick="editName(this, 'theme')">Edit</button>
                                    <button class="apply-theme-name-btn" style="display:none;" onclick="applyName(this, 'theme')">Save</button>
                                    <span asp-validation-for="@Model.ThemeObjects[y].Theme.Name" class="text-danger"></span>
                                </td>
                                <td>
                                    @if (Model.ThemeObjects[y].TagCheckboxes != null)
                                    {
                                        @for (int i = 0; i < Model.ThemeObjects[y].TagCheckboxes.Count; i++)
                                        {
                                            <div class="tag-checkbox">
                                                <input asp-for="@Model.ThemeObjects[y].TagCheckboxes[i].Name" hidden class="tag-checkbox-name" />
                                                <label>@Model.ThemeObjects[y].TagCheckboxes[i].Name</label>
                                                <input asp-for="@Model.ThemeObjects[y].TagCheckboxes[i].CheckedState" class="tag-checkbox-state" />
                                                <input asp-for="@Model.ThemeObjects[y].TagCheckboxes[i].Value" hidden class="tag-checkbox-id" />
                                            </div>
                                        }
                                        <span asp-validation-for="@Model.ThemeObjects[y].TagCheckboxes"></span>
                                    }


                                </td>
                                <td class="text-center">
                                    <div class="w-75 btn-group" role="group">
                                        <button class="btn btn-danger open-delete-theme-modal-btn" value="@Model.ThemeObjects[y].Theme.Id">Delete</button>
                                    </div>
                                </td>
                            </tr>
                        }

                    }

                </tbody>
            </table>

        }
        else
        {
            <p>No theme exists.</p>
        }
        <input type="submit" class="btn btn-info w-100" name="save" value="Save" asp-action="SaveThemes" />
        <button id="open-add-theme-modal-button">Add theme</button>


        <input type="submit" class="btn btn-info w-100" id="myTagBtn" name="openModal" value="Add/Edit Tags" />

        <!-- Add theme modal -->
        <div id="addThemeModal" class="modal add-team-modal" style="display: none;">
            <div class="modal-content">
                <span class="themeModalClose">&times;</span>

                <!-- New theme name input field-->
                <input type="text" id="add-theme-input" asp-for="NewThemeName" />

                <!-- Buttons -->
                <div class="form-group row">
                    <div class="col-8 row">
                        <button id="close-add-theme-modal-button">Cancel</button>
                        <input type="submit" id="add-team-submit-button" class="btn btn-info w-100" name="add" value="Add" asp-action="AddTheme" />
                    </div>
                </div>

            </div>
        </div>

        <!-- Delete theme modal -->
        <div id="deleteThemeModal" class="modal delete-theme-modal" style="display: none;">
            <div class="modal-content">
                <span class="themeModalClose">&times;</span>

                <!-- New team name input field -->
                <p>Are you sure you want to delete this team. All projects under this team will be deleted. All items assigned this team or subsequent projects will be unnasigned.</p>

                <!-- Buttons -->
                <div class="form-group row">
                    <div class="col-8 row">
                        <button id="close-delete-theme-modal-button">Cancel</button>
                        <button id="delete-theme-confirm-btn" asp-action="DeleteTheme" class="btn btn-danger delete-theme" name="deleteId" value="">Delete</button>
                    </div>
                </div>

            </div>
        </div>

    </form>
</div>

<!-- Tag modal -->
<div id="myTagModal" class="modal tag-modal" style="display:none;">
    <div class="modal-content">
        <span class="tagModalClose">&times;</span>
        <p>Some text in the Modal..</p>

        <!-- Tag select list -->
        <div class="form-group row">
            <div class="col-8 row">
                <select id="tag-select-dropdown" asp-items="@tagSelectList" class="form-control">
                    <option disabled selected value="0">-- Select Tag --</option>
                    <option value="newTag">New Tag</option>
                </select>
            </div>
        </div>

        <!-- Tag name input field -->
        <div class="form-group row">
            <div class="col-8 row">
                <input id="tag-name-input" class="form-control" />
                <span class="text-danger"></span>
            </div>
        </div>
        <!-- Buttons -->
        <div class="form-group row">
            <div class="col-8 row">
                <input type="button" id="tag-submit-button" class="btn btn-info w-100 tag-modal-btn" value="Update" />
                <button id="open-delete-tag-modal-btn" class="btn btn-info w-100">Delete</button>
            </div>
        </div>
    </div>
</div>

<!-- Delete tag modal -->
<div id="deleteTagModal" class="modal delete-tag-modal" style="display: none;">
    <div class="modal-content">
        <span class="tagModalClose">&times;</span>

        <!-- New team name input field -->
        <p>Are you sure you want to delete this team. All projects under this team will be deleted. All items assigned this team or subsequent projects will be unnasigned.</p>

        <!-- Buttons -->
        <div class="form-group row">
            <div class="col-8 row">
                <button id="close-delete-tag-modal-button">Cancel</button>
                <input type="button" id="tag-delete-button" class="btn btn-info w-100 tag-modal-btn" value="Delete" />
            </div>
        </div>

    </div>
</div>
